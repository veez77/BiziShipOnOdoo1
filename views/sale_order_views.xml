<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_form_biziship_inherit" model="ir.ui.view">
        <field name="name">sale.order.form.biziship.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="%(BiziShipOnOdoo1.action_biziship_bol_import_wizard)d" string="BiziShip: Process BOL" type="action" class="oe_highlight" invisible="state not in ['draft', 'sent', 'sale']"/>
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page string="LTL Freight Quotes" name="biziship_freight_quotes">
                    <group invisible="not biziship_shipment_id">
                        <group string="BiziShip Booking Details">
                            <field name="biziship_bol_number" readonly="1"/>
                            <field name="biziship_shipment_id" readonly="1"/>
                            <field name="biziship_bol_url" widget="url" readonly="1" text="Printable BOL Document" invisible="not biziship_bol_url"/>
                        </group>
                    </group>
                    <field name="biziship_quote_ids">
                        <tree string="Quotes" create="false" edit="false">
                            <field name="is_selected" widget="boolean_toggle" readonly="1"/>
                            <field name="carrier_name" readonly="1"/>
                            <field name="service_level" readonly="1"/>
                            <field name="transit_days" readonly="1"/>
                            <field name="delivery_date" readonly="1"/>
                            <field name="total_charge" widget="monetary" options="{'currency_field': 'currency_id'}" readonly="1"/>
                            <field name="currency" column_invisible="True" readonly="1"/>
                            <field name="currency_id" column_invisible="True"/>
                            <field name="quote_id_ref" readonly="1"/>
                            <button name="action_select_quote" type="object" string="Select Quote" class="btn-primary" invisible="is_selected"/>
                        </tree>
                        <form string="Quote Details" create="false" edit="false">
                            <sheet>
                                <div class="oe_title">
                                    <h1><field name="carrier_name" readonly="1"/></h1>
                                </div>
                                <group>
                                    <group>
                                        <field name="service_level" readonly="1"/>
                                        <field name="transit_days" readonly="1"/>
                                        <field name="delivery_date" readonly="1"/>
                                    </group>
                                    <group>
                                        <field name="total_charge" widget="monetary" options="{'currency_field': 'currency_id'}" readonly="1"/>
                                        <field name="currency" invisible="1" readonly="1"/>
                                        <field name="currency_id" invisible="1"/>
                                        <field name="quote_id_ref" readonly="1"/>
                                        <field name="is_selected" readonly="1"/>
                                    </group>
                                </group>
                                <notebook>
                                    <page string="Charge Breakdown" name="charge_breakdown">
                                        <field name="quote_details" readonly="1" widget="text" style="font-family: monospace; white-space: pre;"/>
                                    </page>
                                </notebook>
                                <footer>
                                    <button string="Close" class="btn-primary" special="cancel" data-hotkey="z"/>
                                </footer>
                            </sheet>
                        </form>
                    </field>
                    <div class="mt-4 text-center">
                        <field name="biziship_has_selected_quote" invisible="1"/>
                        <button name="action_open_biziship_quote_confirm" type="object" string="Confirm Chosen Quote" class="btn-primary" invisible="not biziship_has_selected_quote"/>
                        <button name="action_open_biziship_quote_confirm" type="object" string="Confirm Chosen Quote" class="btn-secondary" invisible="biziship_has_selected_quote" title="Please select a quote first to confirm it."/>
                    </div>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
